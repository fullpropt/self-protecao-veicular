# ============================================
# SELF PROTEÃ‡ÃƒO VEICULAR - CONFIGURAÃ‡Ã•ES v4.0
# ============================================
# Copie este arquivo para .env e configure os valores

# ConfiguraÃ§Ãµes do Servidor
PORT=3001
HOST=0.0.0.0
NODE_ENV=production
METRICS_ENABLED=false
METRICS_BEARER_TOKEN=

# DiretÃ³rios
SESSIONS_DIR=/mnt/data/sessions
UPLOAD_DIR=./uploads

# Banco de Dados (Postgres)
DATABASE_URL=postgresql://usuario:senha@host:5432/banco
PG_POOL_MAX=20
PG_IDLE_TIMEOUT_MS=30000
PG_CONNECTION_TIMEOUT_MS=10000
PG_QUERY_TIMEOUT_MS=60000
PG_STATEMENT_TIMEOUT_MS=60000
PG_IDLE_IN_TRANSACTION_SESSION_TIMEOUT_MS=60000
PG_KEEPALIVE=true
PG_KEEPALIVE_INITIAL_DELAY_MS=10000

# ============================================
# SEGURANÃ‡A (OBRIGATÃ“RIO EM PRODUÃ‡ÃƒO)
# ============================================

# Chave secreta para tokens JWT (mÃ­nimo 32 caracteres)
# Gere uma chave segura: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua-chave-secreta-super-segura-aqui-min-32-chars

# Chave para criptografia de mensagens (32 caracteres)
ENCRYPTION_KEY=chave-de-criptografia-32-caracteres
# Chave anterior para rotacao assistida (opcional, manter por 7-30 dias)
# Quando rotacionar: ENCRYPTION_KEY recebe a nova chave e ENCRYPTION_KEY_PREVIOUS recebe a chave antiga.
ENCRYPTION_KEY_PREVIOUS=

# Content Security Policy: "off", "report-only" (recomendado para rollout) ou "enforce"
CSP_MODE=report-only
CSP_REPORT_URI=/api/security/csp/report
CSP_REPORT_LOG_COOLDOWN_MS=600000
CSP_REPORT_TRACKER_LIMIT=4000

# ============================================
# WHATSAPP
# ============================================

# ID da sessÃ£o WhatsApp (nÃ£o altere se nÃ£o souber o que estÃ¡ fazendo)
WHATSAPP_DEFAULT_SESSION_ID=default_whatsapp_session
SESSION_ID=

# PersistÃªncia do auth state do Baileys: "db" (recomendado) ou "multi_file"
WHATSAPP_AUTH_STATE_DRIVER=db

# Se o driver "db" falhar, cai para multi_file automaticamente
WHATSAPP_AUTH_STATE_DB_FALLBACK_MULTI_FILE=true

# Timeout do QR Code em milissegundos (padrÃ£o: 60 segundos)
QR_TIMEOUT=60000

# Tentativas mÃ¡ximas de reconexÃ£o
MAX_RECONNECT_ATTEMPTS=5

# Delay entre tentativas de reconexÃ£o (ms)
RECONNECT_DELAY=3000

# Usar versao "latest" do Baileys a cada boot (NAO recomendado em producao)
WHATSAPP_USE_LATEST_BAILEYS_VERSION=false

# Fixar versao do protocolo do Baileys (formato: x.y.z) - opcional
# Exemplo: WHATSAPP_BAILEYS_VERSION=2.3000.102
WHATSAPP_BAILEYS_VERSION=

# Sincronizar historico completo ao conectar (mais pesado; pode aumentar instabilidade)
WHATSAPP_SYNC_FULL_HISTORY=false

# Processar lotes de historico entregues apos reconexao para reduzir lacunas no inbox
WHATSAPP_HISTORY_SYNC_ENABLED=true
WHATSAPP_HISTORY_SYNC_MESSAGES_LIMIT=600

# Timeouts/keepalive da conexao WhatsApp (ms)
WHATSAPP_KEEPALIVE_INTERVAL_MS=15000
WHATSAPP_CONNECT_TIMEOUT_MS=60000
WHATSAPP_DEFAULT_QUERY_TIMEOUT_MS=60000
WHATSAPP_RETRY_REQUEST_DELAY_MS=500

# Janela de aquecimento apos reconexao e backoff de disparo quando a sessao oscila (ms)
WHATSAPP_SESSION_SEND_WARMUP_MS=12000
WHATSAPP_SESSION_DISPATCH_BACKOFF_MS=60000
WHATSAPP_SESSION_RECONNECTING_RETRY_MS=20000
WHATSAPP_SESSION_WARMING_UP_RETRY_MS=10000
WHATSAPP_SESSION_DISCONNECTED_RETRY_MS=60000
WHATSAPP_SESSION_COOLDOWN_RETRY_MS=15000

# Throttle por sessao para evitar disparos excessivos no WhatsApp
WHATSAPP_SESSION_RATE_LIMIT_ENABLED=true
WHATSAPP_SESSION_RATE_LIMIT_MAX_PER_MINUTE=30

# Catch-up adicional pos-reconexao (backfill via store local)
WHATSAPP_RECONNECT_CATCHUP_ENABLED=true
WHATSAPP_RECONNECT_CATCHUP_DELAY_MS=7000
WHATSAPP_RECONNECT_CATCHUP_MAX_CONVERSATIONS=40
WHATSAPP_RECONNECT_CATCHUP_MESSAGES_PER_CONVERSATION=80
WHATSAPP_RECONNECT_CATCHUP_MAX_RUNTIME_MS=45000

# ============================================
# FILA DE MENSAGENS
# ============================================

# Delay entre mensagens em massa (ms) - evita bloqueios
BULK_MESSAGE_DELAY=3000

# MÃ¡ximo de mensagens por minuto
MAX_MESSAGES_PER_MINUTE=30

# Habilita worker da fila nesta instÃ¢ncia (use "false" nos serviÃ§os de teste que compartilham o mesmo banco)
QUEUE_WORKER_ENABLED=true

# Enfileirar mensagens de fluxo (recomendado) e prioridade da fila para fluxos
FLOW_MESSAGE_QUEUE_ENABLED=true
FLOW_MESSAGE_QUEUE_PRIORITY=50

# Habilita worker de automaÃ§Ãµes agendadas nesta instÃ¢ncia
SCHEDULED_AUTOMATIONS_WORKER_ENABLED=true

# Lock de lideranÃ§a via Postgres para evitar mÃºltiplos workers processando a mesma fila em serviÃ§os diferentes
POSTGRES_WORKER_LEADER_LOCK_ENABLED=true
POSTGRES_WORKER_LEADER_LOCK_POLL_MS=5000

# ============================================
# WEBHOOKS
# ============================================

# [LEGADO] Chave global para validar webhooks de entrada
# Recomendado: usar segredo por conta via API (/api/webhooks/incoming/credential/regenerate).
WEBHOOK_SECRET=webhook-secret-key
# [LEGADO] Owner fixo para o WEBHOOK_SECRET global acima.
# Em modo multi-tenant por token, este valor nao e necessario.
WEBHOOK_INCOMING_OWNER_USER_ID=1

# URL padrÃ£o para webhooks de saÃ­da (opcional)
WEBHOOK_URL=

# ============================================
# RATE LIMITING
# ============================================

# Janela de tempo para rate limit (ms)
RATE_LIMIT_WINDOW_MS=60000

# MÃ¡ximo de requisiÃ§Ãµes por janela
RATE_LIMIT_MAX_REQUESTS=100

# Modo de armazenamento do rate limit: "database" (multiplas rÃ©plicas) ou "memory" (instÃ¢ncia Ãºnica)
RATE_LIMIT_STORAGE=database

# ============================================
# BACKUP OPERACIONAL
# ============================================

# Ativa rotina automatica de backup no processo da API
BACKUP_AUTO_ENABLED=false

# Intervalo da rotina em horas (1-720)
BACKUP_INTERVAL_HOURS=24

# ============================================
# UPLOAD DE ARQUIVOS
# ============================================

# Tamanho mÃ¡ximo de arquivo
MAX_FILE_SIZE=50mb

# ============================================
# FUNCIONALIDADES
# ============================================

# Habilitar construtor de fluxos
FLOW_BUILDER_ENABLED=true
FLOW_AWAITING_INPUT_RECOVERY_ENABLED=true
FLOW_AWAITING_INPUT_RECOVERY_INTERVAL_MS=12000
FLOW_EXECUTION_RUNNING_TIMEOUT_HOURS=72
FLOW_INBOUND_TELEMETRY_ENABLED=false
FLOW_INBOUND_TELEMETRY_SLOW_MS=1500
INCOMING_MESSAGE_IDEMPOTENCY_TTL_MS=600000
INCOMING_MESSAGE_LOCK_STALE_MS=120000
FLOW_INBOUND_POSTGRES_LOCK_ENABLED=true
FLOW_INBOUND_POSTGRES_LOCK_NAMESPACE=74121
INCOMING_MESSAGE_RECEIPT_LEASE_MS=120000
INCOMING_MESSAGE_RECEIPTS_RETENTION_MS=604800000
INCOMING_MESSAGE_RECEIPTS_CLEANUP_INTERVAL_MS=600000

# Geracao de fluxo por IA (OpenAI)
OPENAI_API_KEY=sua-chave-api-openai
OPENAI_FLOW_MODEL=gpt-5-mini
OPENAI_API_BASE_URL=https://api.openai.com/v1
OPENAI_FLOW_TIMEOUT_MS=25000

# Classificador de intencao por IA (Gemini) para gatilhos keyword
FLOW_INTENT_CLASSIFIER_ENABLED=true
GEMINI_API_KEY=sua-chave-api-gemini
GEMINI_MODEL=gemini-2.0-flash-lite
FLOW_INTENT_CLASSIFIER_MIN_CONFIDENCE=0.70
FLOW_INTENT_CLASSIFIER_STRICT=false
FLOW_INTENT_TRIGGER_DEFAULT_AFTER_ATTEMPTS=2
FLOW_INTENT_CLASSIFIER_MIN_CANDIDATES=1
FLOW_INTENT_CLASSIFIER_MAX_CANDIDATES=5
GEMINI_REQUEST_TIMEOUT_MS=4500
GEMINI_QUOTA_BACKOFF_MS=600000
FLOW_INTENT_FUZZY_THRESHOLD=0.34
FLOW_INTENT_FUZZY_MIN_SCORE=0.58
FLOW_INTENT_FUZZY_MIN_TOKEN_COVERAGE=0.45

# PolÃ­tica de erro das output actions em fluxos:
# continue = sempre segue; required = falha sÃ³ quando aÃ§Ã£o Ã© marcada como crÃ­tica; fail_all = qualquer erro encerra fluxo
FLOW_OUTPUT_ACTION_ERROR_MODE=required

# URL do Frontend
FRONTEND_URL=https://seu-app.railway.app

# URL publica usada para links de confirmacao de email (preferencial)
APP_URL=https://seu-app.railway.app

# ============================================
# PAGAR.ME
# ============================================

# Chave secreta da API do Pagar.me
PAGARME_SECRET_KEY=sk_live_xxx

# Chave publica/appId opcional para tokenizacao do cartao direto no navegador
PAGARME_PUBLIC_KEY=pk_live_xxx

# Base URL opcional da API (normalmente nao precisa alterar)
PAGARME_API_BASE_URL=

# IDs dos planos recorrentes usados no checkout publico
PAGARME_PLAN_STARTER=plan_xxx
PAGARME_PLAN_PREMIUM=plan_xxx
PAGARME_PLAN_ADVANCED=plan_xxx

# Email de suporte/comercial usado no plano Monster
SALES_SUPPORT_EMAIL=suporte@zapvender.com

# ============================================
# EMAIL TRANSACIONAL (MAILMKT)
# ============================================

# URL base do MailMKT (sem o path final)
MAILMKT_URL=https://mailmkt.seudominio.com

# Chave server-to-server para integracao ZapVender -> MailMKT
MAILMKT_INTEGRATION_API_KEY=sua-chave-server-to-server

# Timeout da chamada ao MailMKT em ms (opcional)
MAILMKT_REQUEST_TIMEOUT_MS=10000

# Texto exibido no email sobre expiracao do link (opcional)
EMAIL_CONFIRMATION_EXPIRES_TEXT=24 horas

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================

# Origens permitidas (separadas por vÃ­rgula)
# Exemplo: http://localhost:3000,https://app.exemplo.com
# Use * para permitir todas (NÃƒO RECOMENDADO EM PRODUÃ‡ÃƒO)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
